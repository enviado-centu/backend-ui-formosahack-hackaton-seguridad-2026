# Versiones fijas: una instalación limpia con `pip install -r requirements.txt` tiene que funcionar.

# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pydantic[email]==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
httpx==0.28.1

# Base de datos (el extra asyncio instala greenlet, necesario para SQLAlchemy async)
sqlalchemy[asyncio]==2.1.0
greenlet==3.5.6
alembic==1.20.0
aiosqlite==0.22.1

# Autenticación
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 no es compatible con bcrypt >= 4.1 (el registro falla con
# "password cannot be longer than 72 bytes")
bcrypt==4.0.1

# MODULO-PY (motor de reglas y modelo de ML)
tldextract==5.3.2
joblib==1.6.0
numpy==2.5.3
pandas==3.0.6
scikit-learn==1.9.1
